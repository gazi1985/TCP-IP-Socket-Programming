TCP传输相关：
1.I/O缓冲在每个TCP套接字中单独存在
2.I/O缓冲在创建套接字时自动生成
3.即使关闭套接字也会继续传递【输出】缓冲中遗留的数据
4.关闭套接字将丢失输入缓冲中数据

TCP不存在缓冲溢出的问题：TCP中存在滑动窗口（Sliding Window）协议

发送函数write(Linux)/Send(Windows)并不会在完成向对方主机的数据传输时返回，而是在数据移出到输出缓存时。TCP会保证输出缓存数据的传输正确。